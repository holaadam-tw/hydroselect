## 1. 基礎建設

- [x] 1.1 安裝依賴：`npm install jspdf jspdf-autotable`
- [x] 1.2 建立 `src/data/productCatalog.js`，定義產品型錄資料結構與所有品牌的產品資料（Propiston、Yuken、Rexroth、Parker、北部精機）

## 2. 產品型號匹配引擎

- [x] 2.1 建立 `src/utils/productMatcher.js`，實作型號匹配核心邏輯（向上取整匹配規則）
- [x] 2.2 實作泵匹配：根據排量與最大壓力匹配各品牌泵型號
- [x] 2.3 實作閥件匹配：分別匹配方向閥（流量）和溢流閥（壓力）
- [x] 2.4 實作油壓缸匹配：根據缸徑、行程匹配各品牌缸型號
- [x] 2.5 實作無匹配時的「超出產品範圍」提示處理

## 3. UI 整合 — 匹配結果顯示

- [x] 3.1 建立 `src/components/ProductMatchResult.js`，實作品牌卡片升級 UI（顯示具體型號 + 規格參數）
- [x] 3.2 在 App.js 中整合匹配引擎：計算完成後自動呼叫 `productMatcher` 取得匹配結果
- [x] 3.3 升級各元件分頁（泵/閥/缸/電機）的品牌卡片，顯示匹配到的具體型號
- [x] 3.4 升級「總報告」分頁，顯示每個品牌的完整選型方案

## 4. PDF 生成功能

- [x] 4.1 準備中文字型：取得 Noto Sans TC 字型子集檔案，轉為 jsPDF 可用的 base64 格式
- [x] 4.2 建立 `src/utils/pdfGenerator.js`，實作 PDF 生成基礎架構（頁面尺寸、字型載入、公司抬頭排版）
- [x] 4.3 實作 Propiston 正式報價單 PDF：公司資訊、客戶名稱、產品明細表（型號/規格/數量/單價/小計）、合計金額、備註
- [x] 4.4 實作其他品牌建議規格書 PDF：標題「液壓系統建議規格書」、型號與規格參數表、頁尾免責聲明

## 5. UI 整合 — PDF 按鈕與互動

- [x] 5.1 在 Propiston 品牌卡片新增「生成報價單」按鈕（僅計算完成後顯示）
- [x] 5.2 在其他品牌卡片新增「匯出建議規格」按鈕（僅計算完成後顯示）
- [x] 5.3 實作「生成報價單」點擊流程：彈出客戶名稱輸入 → 生成 PDF → 觸發下載（檔名 `Propiston_報價單_YYYYMMDD.pdf`）
- [x] 5.4 實作「匯出建議規格」點擊流程：直接生成 PDF → 觸發下載

## 6. 測試與驗收

- [x] 6.1 驗證各品牌型號匹配正確性（以吹瓶機、裁斷機、彎管機三個預設場景測試）
- [x] 6.2 驗證 PDF 中文字顯示正確、排版整齊
- [x] 6.3 驗證無匹配結果時的錯誤提示正確顯示
- [x] 6.4 驗證 `npm run build` 可正常建置
